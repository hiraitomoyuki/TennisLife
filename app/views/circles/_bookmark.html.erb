<% if circle.bookmark_by?(current_user) %>
  <%= link_to "ブックマークを外す", circle_bookmarks_path(circle.id), method: :delete, remote: true, class: "btn btn-primary btn-block" %>
<% else %>
  <%= link_to "ブックマークする", circle_bookmarks_path(circle.id), method: :post, remote: true, class: "btn btn-primary btn-block" %>
<% end %>  